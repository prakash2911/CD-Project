import 'package:flutter/cupertino.dart';

class classDetails{
  String regNo;
  String course;
  String stream;
  String year;
  String name;
  classDetails(
      {required this.regNo,
        required this.course,
        required this.stream,
        required this.year,
        required this.name
        });
 // Map toJson() =>
}